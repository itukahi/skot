<h1>Rooms#index</h1>
<p>Find me in app/views/rooms/index.html.erb</p>

<ul>
<% @targets.each do |target| %>
    <li><%= link_to target.janru, rooms_show_path(target_id: target) %></li>
<% end %>
<%= link_to 'New Target', new_target_path %>
</ul>

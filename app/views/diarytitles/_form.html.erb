<%= form_with(model: diarytitle) do |form| %>
  <% if diarytitle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(diarytitle.errors.count, "error") %> prohibited this diarytitle from being saved:</h2>

      <ul>
        <% diarytitle.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
